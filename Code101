884. Uncommon Words from Two Sentences
Solution-
class Solution(object):
    def uncommonFromSentences(self, s1, s2):
        s=s1.split()+s2.split()
        res=[]
        for i in s:
            if s.count(i)==1:
                res.append(i)
        return(res)
                
